**/.idea
**/~$*
**/*.txt

# 例外: 授权/许可类 txt 必须能提交
# 本项目是 AGPL v3 的修改版, 且内附 SIL OFL 1.1 字体,
# 两者的授权文件都需要随代码一并分发, 不能被上面的规则忽略
!**/LICENSE*.txt
!**/*LICENSE*.txt
!**/*OFL*.txt
!**/*COPYING*.txt

# Godot 版本升级时自动生成的整份项目备份(体积很大), 不纳入版本管理
枪火地牢-*/
*-([0-9].[0-9])/

# ── 主菜单随机背景（10 张 CraftPix 免费素材）──────────────────────────────
# CraftPix 免费素材条款允许商用、允许随游戏出售, 但 §2.2.1 禁止【再分发美术源文件】。
# 本仓库是公开仓库, 所以这 10 张图【不上传】—— 只随打包发行（导出时从本机磁盘打进 pck）。
# 效果: 仓库里没有它们, 别人 clone 下来跑会退回视差大厅背景（代码里有兜底, 不会黑屏）;
#       packaged 版本里则一切正常。
# 本机怎么补齐: resource/sprite/ui/mainBackground/variants/README.md
#
# 注意: 同目录的 LICENSE.md(授权记录) 必须入库, 上面的规则只针对图片本身。
GunfireDungeon_Godot/resource/sprite/ui/mainBackground/variants/*.png
GunfireDungeon_Godot/resource/sprite/ui/mainBackground/variants/*.png.import

# ── AK47 武器贴图（同一套 CraftPix 免费素材）───────────────────────────────
# 和上面同理: §2.2.1 禁止再分发美术源文件, 而本仓库是公开仓库。
# 该图由 CraftPix「Free Guns Icon 32x32 Pixel Pack」的 Icon29_37.png 旋转 39.4°
# 转成侧视得到, 属于"修改版素材", 同样不上传。
# 本机怎么补齐: resource/sprite/weapon/weapon0047/restore_ak47.ps1
# 注意: 同目录的 LICENSE.md(授权记录) 必须入库。
GunfireDungeon_Godot/resource/sprite/weapon/weapon0047/*.png
GunfireDungeon_Godot/resource/sprite/weapon/weapon0047/*.png.import

# ── 不死刽子手 / 刽子手的幽灵 精灵图（Kronovi- 的 itch 免费素材）──────────────
# 页面授权: 可商用、可修改、不要求署名, 但 "redistributing and reselling the sprite
# are restricted" —— 禁止再分发。本仓库是公开仓库, 所以 PNG 不进库, 只随 pck 发行。
# 后果: clone 下来这只精英怪会进随机池却画不出来（武器贴图那批同理, 没有代码兜底）。
# 本机怎么补齐: resource/sprite/role/executioner0001/restore_sprites.ps1
#              （纯 PowerShell, 复制改名 + 重新生成 64x64 图标）
# 注意: 两个目录的 LICENSE.md(授权记录) 与 restore 脚本本身必须入库。
GunfireDungeon_Godot/resource/sprite/role/executioner0001/*.png
GunfireDungeon_Godot/resource/sprite/role/executioner0001/*.png.import
GunfireDungeon_Godot/resource/sprite/role/spirit0001/*.png
GunfireDungeon_Godot/resource/sprite/role/spirit0001/*.png.import
